// Glyph-list sidebar widths (px).
export const GLYPH_LIST_INITIAL_WIDTH_PX = 192;
export const GLYPH_LIST_MIN_WIDTH_PX = 120;
export const GLYPH_LIST_MAX_WIDTH_PX = 400;

// Sidebar collapses to its icon-only mode below this width during drag.
export const GLYPH_LIST_COLLAPSE_THRESHOLD_PX = 80;

// Thumbnail tile in the glyph list (px, SVG viewBox).
export const GLYPH_THUMBNAIL_SIZE_PX = 28;
